version: 2.1
# comandos reutilizables en otros jobs.

jobs:
  saluda:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo hola $NOMBRE
      - run : Mi ID de job es $CIRCLE_WORKSPACE_ID
workflows:
  workflow1:
    jobs:
      - saluda